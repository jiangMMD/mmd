<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/rabbit
        http://www.springframework.org/schema/rabbit/spring-rabbit.xsd">


    <rabbit:connection-factory id="rabbitMQConnectionFactory"  host="${rabbitmq.host}"
                               port="${rabbitmq.port}" password="${rabbitmq.password}"/>

    <rabbit:admin connection-factory="rabbitMQConnectionFactory"/>
    <!-- durable:是否持久化 ；      exclusive: 仅创建者可以使用的私有队列，断开后自动删除；
     auto_delete: 当所有消费客户端连接断开后，是否自动删除队列  -->
    <rabbit:queue id="queue" name="mmt_message" durable="true" auto-delete="false" exclusive="false"/>

    <bean id="queueListenter" class="com.mmt.listener.AmqListener"/>

    <!-- acknowledge  manual手动应答模式 -->
    <rabbit:listener-container connection-factory="rabbitMQConnectionFactory" acknowledge="auto">
        <rabbit:listener ref="queueListenter" queues="queue"/>
    </rabbit:listener-container>

</beans>